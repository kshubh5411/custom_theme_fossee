
{%
  set title_classes = [
    label_display == 'visually_hidden' ? 'visually-hidden',
  ]
%}

{% if label_hidden %}
 
      {% for item in items %}
        <iframe src="{{item.content}}?autoplay=0&showinfo=0&controls=0" frameborder="0" allowfullscreen></iframe>
      {% endfor %}

{% endif %}
